[build]
  base = "beekeeper-dev"
  command = "npm run build"
  publish = "dist"

# Force Netlify to use a stable Node version (avoids Rollup native bug)
[build.environment]
  NODE_VERSION = "20"

# You can omit [functions] altogether if you’re not using Netlify Functions
# (Supabase edge functions are separate)
